name: RDP-Kali

on:
  workflow_dispatch:

jobs:
  secure-rdp-kali:
    runs-on: ubuntu-latest
    timeout-minutes: 3600

    steps:
      - name: Install XFCE Desktop and RDP Server
        run: |
          # Update system
          sudo apt-get update -y
          
          # Install XFCE desktop environment (ligero para Kali)
          sudo DEBIAN_FRONTEND=noninteractive apt-get install -y xfce4 xfce4-goodies
          
          # Install xrdp server
          sudo apt-get install -y xrdp
          
          # Configure xrdp to use XFCE
          echo "xfce4-session" | sudo tee /etc/xrdp/startwm.sh
          sudo chmod +x /etc/xrdp/startwm.sh
          
          # Restart xrdp service
          sudo systemctl restart xrdp
          sudo systemctl enable xrdp

      - name: Install Additional Tools (Opcional)
        run: |
          # Herramientas útiles para Kali
          sudo apt-get install -y \
            firefox-esr \
            terminator \
            nautilus \
            vim \
            gedit \
            thunar \
            xfce4-terminal

      - name: Configure RDP Security Settings
        run: |
          # Configurar xrdp para permitir conexiones
          sudo sed -i 's/port=3389/port=3389/g' /etc/xrdp/xrdp.ini
          
          # Permitir en el firewall (ufw)
          sudo ufw allow 3389/tcp
          sudo ufw reload

      - name: Create RDP User with Secure Password
        run: |
          # Generar contraseña segura
          PASSWORD=$(openssl rand -base64 12 | tr -d '/+=' | head -c 16)
          
          # Crear usuario
          sudo useradd -m -s /bin/bash rdpuser
          echo "rdpuser:$PASSWORD" | sudo chpasswd
          
          # Agregar al grupo sudo (opcional)
          sudo usermod -aG sudo rdpuser
          
          # Guardar credenciales
          echo "RDP_CREDS=User: rdpuser | Password: $PASSWORD" >> $env:GITHUB_ENV

      - name: Install Tailscale
        run: |
          # Instalar Tailscale
          curl -fsSL https://tailscale.com/install.sh | sudo sh
          
          # Iniciar Tailscale
          sudo tailscale up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} --hostname=kali-runner-${{ github.run_id }}
          
          # Esperar IP
          sleep 10
          TS_IP=$(tailscale ip -4)
          echo "TAILSCALE_IP=$TS_IP" >> $env:GITHUB_ENV

      - name: Verify RDP Accessibility
        run: |
          echo "Tailscale IP: $env:TAILSCALE_IP"
          
          # Verificar que xrdp está corriendo
          sudo systemctl status xrdp --no-pager
          
          # Test de conectividad
          nc -zv localhost 3389
          
          echo "RDP está listo para conexiones!"

      - name: Maintain Connection
        run: |
          echo "=== RDP ACCESS ==="
          echo "Address: $env:TAILSCALE_IP"
          echo "Username: rdpuser"
          echo "Password: $(echo $env:RDP_CREDS)"
          echo "=================="
          
          # Mantener el runner activo
          while true; do
            echo "[$(date)] RDP Active - Use Ctrl+C in workflow to terminate"
            sleep 300
          done
